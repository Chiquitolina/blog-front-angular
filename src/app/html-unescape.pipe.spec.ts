import { HtmlUnescapePipe } from './html-unescape.pipe';

describe('HtmlUnescapePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlUnescapePipe();
    expect(pipe).toBeTruthy();
  });
});
